<nav class="flex items-center justify-between p-3 bg-stone-100 mb-8">
  <div class="flex items-center text-stone-600">
    <%= link_to "TripAware", root_path, 
    class: "font-semibold text-2xl"%>
  </div>

  <div class="flex items-center space-x-4 font-mono text-lg text-stone-700">
    <% if user_signed_in? %>
        <%= link_to 'Edit profile', edit_user_registration_path,
         target: "_top", class: "text-teal-500 underline" %>
        <span class="ml-3 text-blue-500"><%= current_user.username %><?span>
        <%= link_to "log out", destroy_user_session_path, data: {turbo_method: :delete,
        turb_frame: "_top"}, class: "text-blue-700 font-bold underline" %>
    <% else %>
        <%= link_to "log in", new_user_session_path, target: "_top",
        class: "text-teal-500 underline"  %>
        <%= link_to "Sign up", new_user_registration_path, target: "_top", 
        class: "text-blue-700 font-bold underline"  %>
    <% end %>
  </div>
</nav>
